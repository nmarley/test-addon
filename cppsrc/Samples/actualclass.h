class ActualClass {
public:
    ActualClass(double value); // constructor
    double getValue();         // getter
    double add(double toAdd);  // adds the toAdd value to the value
private:
    double value;
}
